"""Custom MCP task suite (Phase 3): a purpose-built filesystem-shaped MCP server
over a controlled, resettable backend, so cascading-failure trials can be graded
deterministically the way tau_retail grades order mutations."""
